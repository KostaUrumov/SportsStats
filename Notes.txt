Next steps:
1.Edit match (change result, team etc). If we try to remove a team from a tournament should not be possible if team has a match already.
2.Standings of a group and a single group tournaments.